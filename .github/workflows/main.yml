name: Deploy high availability AWS infrastructure

on:
  workflow_dispatch:

jobs:
  terraform:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5.0.1

      - name: Set up AWS credentials
        run: |
          echo "AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }}" >> $GITHUB_ENV
          echo "AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_ACCESS_KEY }}" >> $GITHUB_ENV
          echo "AWS_REGION=${{ secrets.AWS_REGION }}" >> $GITHUB_ENV

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3.1.2
        with:
          terraform_version: "1.13.4"

      - name: Terraform Init
        run: |
          cd Terraform
          terraform init

      - name: Terraform Plan
        env:
          AWS_REGION: ${{ secrets.AWS_REGION }}
        run: |
          cd Terraform
          terraform validate
          terraform plan -var="aws_region=${{ secrets.AWS_REGION }}" -out=tfplan

      - name: Terraform Apply
        env:
          AWS_REGION: ${{ secrets.AWS_REGION }}
        run: |
          cd Terraform
          terraform apply -var="aws_region=${{ secrets.AWS_REGION }}" -auto-approve tfplan

      - name: Terraform Output
        id: tf_output
        run: |
          cd Terraform
          ASG_IPS=$(terraform output -json asg_instance_ips | jq -r '.[]' | paste -sd " " -)
          echo "ASG_IPS=$ASG_IPS" >> $GITHUB_ENV

      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

      - name: Install Ansible AWS dependencies
        run: |
          python3 -m pip install --upgrade pip
          pip install ansible boto3 botocore

      - name: Debug EC2 instances (describe-instances)
        run: |
          aws ec2 describe-instances \
            --region $AWS_DEFAULT_REGION \
            --query "Reservations[].Instances[].{ID:InstanceId,State:State.Name,Name:Tags[?Key=='Name']|[0].Value,Role:Tags[?Key=='Role']|[0].Value,PrivateIP:PrivateIpAddress,PublicIP:PublicIpAddress,Subnet:SubnetId}"
        env:
          AWS_DEFAULT_REGION: ${{ secrets.AWS_REGION }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: Debug EC2 tags
        run: |
          aws ec2 describe-tags --region $AWS_DEFAULT_REGION --query "Tags[?Key=='Name' || Key=='Role']"
        env:
          AWS_DEFAULT_REGION: ${{ secrets.AWS_REGION }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: Debug Ansible inventory (verbose)
        run: |
          cd Ansible
          ANSIBLE_DEPRECATION_WARNINGS=False ANSIBLE_DEBUG=True ansible-inventory -i aws_ec2.yml --list -vvv
        env:
          AWS_DEFAULT_REGION: ${{ secrets.AWS_REGION }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: Run Ansible Playbook
        run: |
          cd Ansible
          ansible-playbook playbook.yml --user ubuntu
        env:
          AWS_DEFAULT_REGION: ${{ secrets.AWS_REGION }}
  
